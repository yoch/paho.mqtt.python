name: mqttnext publish many finalization

on:
  pull_request:
    branches:
      - agent/mqttnext-core-perf-optimizations
    paths:
      - mqttnext/tools/finalize_publish_many_api.py
      - .github/workflows/mqttnext-publish-many-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]' && github.head_ref == 'agent/mqttnext-publish-many'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/mqttnext-publish-many
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - working-directory: mqttnext
        run: pip install -e '.[dev]'
      - run: python mqttnext/tools/finalize_publish_many_api.py
      - working-directory: mqttnext
        run: |
          ruff format src tests benchmarks
          ruff check src tests benchmarks
          mypy src
          PYTHONPATH=. pytest tests/unit -q
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'perf: bound aggregate batch receipt memory'
          git push origin HEAD:agent/mqttnext-publish-many
